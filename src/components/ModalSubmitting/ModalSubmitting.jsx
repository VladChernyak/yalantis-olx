import React from 'react';
import { Loader } from '../';
import './ModalSubmitting.scss';

const ModalSubmitting = () => (
  <div className="modal-submitting">
    <Loader />
  </div>
);

export default ModalSubmitting;
